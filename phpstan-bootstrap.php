<?php

// PHPStan bootstrap: makes the plugin's own constants visible to static analysis
// (mirrors what the main plugin file defines at runtime).

define( 'BE_PLUS_SCSS_COMPILER_MAIN_FILE', __FILE__ );
