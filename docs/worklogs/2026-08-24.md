# 2026-08-24

## Summary

Prepared `readme.txt` for the WordPress.org submission of version 1.0.0.
No code changes; this was a documentation-only pass driven by a review of
the wp.org submission requirements.

## Changes

### `readme.txt`
- `Contributors`: now lists both WordPress.org usernames
  (`bearsthemes, tienbeplus`) — previously a placeholder (`beplus`).
- `Tags`: expanded to the maximum of five by adding `preprocessor`.
- Installation step 3 corrected: the admin entry point is
  *Settings → Beplus SCSS Compiler* (the readme said "Beplus SCSS").
- New `Description` bullets covering multiple pairs, fingerprinted
  auto-compilation, source maps/minification, atomic writes,
  child-theme awareness and the `beplus_scss/*` filters.
- New `Frequently Asked Questions` section (path resolution, enqueue
  safety, child themes, failed-compile behaviour).
- `Changelog` 1.0.0 expanded from "Initial release." to a detailed
  feature list (compilation/mirror structure, pairs, fingerprinted auto
  mode, manual mode, source maps/minify, opt-in enqueue, atomic writes,
  settings validation, developer filters).

## Notes

- `Tested up to: 7.1` verified correct — WordPress 7.1 shipped on
  2026-08-19.
- `Text Domain` / `Domain Path` already present in the main plugin header
  (`beplus-scss-compiler.php`); they are not readme.txt fields.

## Outstanding (submission prep)

- Final verification run: `composer lint`, `composer analyse`,
  `composer test`, integration suite in wp-env.
- Official Plugin Check audit of the current build inside wp-env.
- Regenerate `languages/beplus-scss-compiler.pot` before packaging.
- Build the zip (`npm run build:package`) and smoke-test it in wp-env.
- wp.org directory assets (banner/icon) deferred until after approval.
