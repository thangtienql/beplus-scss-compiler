# 2026-08-18

## Summary
Pinned the design contract and release tooling so the plugin can be built by an
agent with zero guessing, and added the daily work-log convention.

## Changes
- Rewrote `Plugin.md` with every decision pinned (zero-guessing rule): namespace
  layout, settings option + keys, default values, web-root/URL formulas,
  fingerprint algorithm, handle format, rewrite endpoint, six `beplus_scss/*`
  filters, main plugin file header, uninstall list, and a Conventions & Naming
  appendix (Appendix A).
- Tooling: `scripts/build-package.mjs` (archiver) + `npm run build` replaces the
  old shell build; `composer test` now runs the unit suite only; added
  `test:integration` and `i18n:pot`; phpunit config + `tests/bootstrap.php`
  updated; `/build/` gitignored.
- Agile docs/rules: removed the "(Chatting in Vietnamese…)" clause; added
  "agent never commits/pushes" rule; fixed the false "opencode.json enforces
  Plugin.md approval" claim and the non-existent "CI gate" claim; aligned
  `wp-plugin-dev` and `debugging` skills with `Settings\SettingsPage` + `Plugin`
  glue naming.
- Added the daily work-log convention (`docs/worklogs/YYYY-MM-DD.md`) and wired
  it into `project-workflow`, `verify-code`, and `pre-push-checklist`.

## Files
- AGENTS.md, Plugin.md, README.md
- composer.json, package.json, package-lock.json, phpunit.xml.dist,
  .gitignore, scripts/build-package.mjs, tests/bootstrap.php
- .opencode/skills/{project-workflow,verify-code,pre-push-checklist,wp-plugin-dev,debugging}/SKILL.md

## Verification
- `composer lint`, `composer analyse`, `composer test` — all green.

## Next
- Enter build planning (Checkpoint 1): TDD plan S1–S11, then implement the
  plugin layers.