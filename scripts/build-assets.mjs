// Placeholder for frontend asset compilation (JS/CSS).
// Today the plugin ships no bundled assets; when one is added, run its
// compiler here (e.g. esbuild/vite) and emit into the expected output dir.

console.log("build-assets: no frontend assets to compile (placeholder).");
